=IMPORTS DE PartnersModal (path)=
(aucun import - ORPHELIN confirme)

=npm rm resend=

To address all issues (including breaking changes), run:
  npm audit fix --force

Run `npm audit` for details.

=resend encore dans package.json ?=
